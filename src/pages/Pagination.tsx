import * as React from 'react'
import { PageLayout } from '../components/PageLayout'

const Pagination = () => {
  return (
    <PageLayout
      title="Pagination"
      sourcePath="/tree/main/src/Pagination"
      description="Displays the page layout based on the total number of pages and the current page number."
      codeSandboxPath="pagination-ucz2v"
    />
  )
}

export default Pagination
